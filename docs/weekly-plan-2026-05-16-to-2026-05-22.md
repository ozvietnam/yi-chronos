# Kế hoạch phát triển tuần 2 (2026-05-16 → 2026-05-22)

## Chủ đề tuần: Product Core + Western Astrology Layer

Tuần 1 (2026-05-09–15) đã ổn định nền tảng: 52 test pass, API contract rõ,
64 quẻ đủ 2 nguồn, pipeline audit tự động.

Tuần này có **2 mục tiêu lớn song song**:

1. **Sản phẩm thực** — thay thế từng phần placeholder bằng logic tính toán đúng.
2. **Trang 1 nâng cấp tối đa** — thêm lớp chiêm tinh phương Tây cho Universe Now.

Spec chi tiết của lớp chiêm tinh: [spec-universe-now-western-astrology.md](spec-universe-now-western-astrology.md).

---

## 0) Cho người mới tham gia / handoff giữa tuần

Dự án có nhiều bên tham gia. Khi bàn giao **bất kỳ lúc nào**, làm 4 bước:

1. **Trạng thái test cuối cùng:** chạy và copy output:
   ```bash
   PYTHONPATH=. pytest tests -q --tb=no | tail -5
   ```
2. **File đã chạm:** `git status` + `git diff --stat HEAD`.
3. **Quyết định trong ngày:** ghi vào mục "Decision log" cuối file này.
4. **Việc đang dở / blocker:** ghi vào mục "Open handoff" cuối file này.

**Tài liệu cần đọc trước khi pick up:**
- [README.md](../README.md) — chạy stack cục bộ.
- [deep-research-report.md](../deep-research-report.md) — bản sắc dự án.
- [docs/rulesets/bac_phai_v1.md](rulesets/bac_phai_v1.md) — ruleset Đông phương.
- [docs/spec-universe-now-western-astrology.md](spec-universe-now-western-astrology.md) — spec lớp Tây phương.
- File handoff gần nhất trong `docs/session-handoff-*.md`.

---

## 1) Hiện trạng đầu vào (đánh giá trước khi bắt đầu)

| Module | Tình trạng | Vấn đề cần giải quyết |
|---|---|---|
| `engine/personal.py` | Placeholder | `five_element_bias` dùng hash ngẫu nhiên — không phải tính từ Can-Chi thực |
| `engine/personal.py` | Placeholder | `generate_milestone_prompts` hardcode 3 mốc tuổi (21/27/33), strength ngẫu nhiên |
| `engine/resonance.py` | Placeholder | 12 dòng, trọng số cứng, không có Thiên–Địa–Nhân tách biệt |
| `engine/scoring.py` | Placeholder | 14 dòng, chưa tích hợp signals từ collector thật |
| `collectors/` | Chưa nối | `jpl_horizons.py` và `noaa_swpc.py` chưa được gọi trong runtime Universe Now thực |
| Feedback DB | Chưa đủ | Có `store_json` nhưng chưa có query/retrieve endpoint |
| Frontend 3 màn hình | Chưa test E2E | Component tồn tại nhưng chưa có test end-to-end flow |
| **Trang 1 — chiêm tinh** | **Chưa có** | **Cần thêm lớp Western astrology theo spec** |

---

## 2) Mục tiêu tuần

### Track A — Product Core (Day 1–4)
1. **Personal Vector thực** — `five_element_bias` tính từ Can-Chi tứ trụ.
2. **Milestone thực** — Sinh mốc theo chu kỳ 10 năm đại vận.
3. **Scoring engine đủ Thiên–Địa–Nhân** — 3 trục rõ ràng.
4. **Feedback loop hoàn chỉnh** — lưu + truy xuất phản hồi.

### Track B — Western Astrology Layer (Day 5–6)
5. **Sky Engine** — tính vị trí 10 thiên thể, 12 cung, aspects, retrograde.
6. **Universe Now nâng cấp** — `/api/sky-now` + UI `SkyChartPanel.vue` + nối collector NOAA/JPL thực.

### Track C — Stabilize & Handoff (Day 7)
7. **Test E2E + Handoff** — chạy đủ luồng người dùng, viết bàn giao tuần 3.

---

## 3) Kế hoạch theo ngày

### Day 1 — Mon 16/05 · Personal Vector thực

**Mục tiêu:** `five_element_bias` dùng Can-Chi tứ trụ thay hash ngẫu nhiên.

- Đọc `ChronosState.ganzhi` (Can-Chi năm/tháng/ngày/giờ) từ ngày sinh.
- Map Can lên ngũ hành: Giáp/Ất→Mộc, Bính/Đinh→Hỏa, Mậu/Kỷ→Thổ, Canh/Tân→Kim, Nhâm/Quý→Thủy.
- Tính `five_element_bias` từ tổng Can tứ trụ (4 trục), chuẩn hóa về tổng = 1.
- Đảm bảo cùng input → cùng output (deterministic 100%).
- Thêm test regression với ít nhất 3 ngày sinh cố định.

**Deliverable:**
- `engine/personal.py` — `five_element_bias` mới.
- `tests/test_personal_vector.py` — test deterministic + spot-check.

**Handoff signal:** `pytest tests/test_personal_vector.py -q` pass.

---

### Day 2 — Tue 17/05 · Scoring engine Thiên–Địa–Nhân đủ trục

**Mục tiêu:** `resonance.py` và `scoring.py` có 3 trục độc lập, có thể hiển thị trên UI.

- Tách `scoring.py`:
  - `score_heaven(kp_norm, moon_illumination, solar_longitude)` → `[0,1]`
  - `score_earth(solar_term_id, season_phase)` → `[0,1]`
  - `score_instability(kp_norm, moon_illumination)` → `[0,1]`
- Cập nhật `resonance.py`:
  - `calculate_sync_score(personal_vector, heaven, earth, instability)` → `SyncResult` gồm:
    - `sync_index` (tổng), `heaven_score`, `earth_score`, `instability_score`, `dominant_element`.
- Cập nhật API `/api/universe-now` và `/api/personal-profile` trả về 3 trục.
- Thêm test: output range hợp lệ, không có NaN/None.

**Deliverable:**
- `engine/scoring.py`, `engine/resonance.py` nâng cấp.
- Test coverage cho scoring + resonance.

**Handoff signal:** `pytest tests/test_scoring.py tests/test_resonance.py -q` pass.

---

### Day 3 — Wed 18/05 · Life Pattern Replay — Milestone theo chu kỳ thực

**Mục tiêu:** Sinh mốc dựa trên chu kỳ 10 năm đại vận, không hardcode tuổi.

- Xây hàm `generate_davan_windows(birth_year, birth_ganzhi)`:
  - Tính các chu kỳ 10 năm đại vận từ năm sinh.
  - Mỗi cửa sổ gắn với một trạng thái Can-Chi → tín hiệu ngũ hành cụ thể.
  - Trả về list mốc: `window_start`, `window_end`, `element_phase`, `signal_strength` (deterministic), `domain`.
- Tối đa 6 mốc cho 1 profile (khoảng 20–80 tuổi).
- Cập nhật `/api/personal-profile` để dùng `generate_davan_windows`.
- Test: cùng ngày sinh → cùng danh sách mốc.

**Deliverable:**
- `engine/personal.py` — `generate_davan_windows()` thay `generate_milestone_prompts()`.
- API response mẫu cập nhật trong `docs/api-contracts.md`.
- Test regression.

**Handoff signal:** `pytest tests/test_personal_vector.py::test_davan_windows -q` pass.

---

### Day 4 — Thu 19/05 · Feedback DB + API đủ để khép vòng

**Mục tiêu:** Người dùng phản hồi mốc → lưu được → truy xuất được.

- Kiểm tra `data/database.py`: xác nhận `store_json` lưu đúng nơi.
- Thêm endpoint:
  - `GET /api/feedback/history?profile_hash=...` — danh sách phản hồi.
  - Cập nhật `POST /api/feedback`: lưu đủ `prompt_id`, `profile_hash`, `feedback_value`, `note`, `timestamp_utc`.
- Thêm schema validation trong `api/schemas.py`.
- Test:
  - POST feedback → `201`.
  - GET history hash đúng → list không rỗng.
  - GET history hash sai → `[]`.

**Deliverable:**
- `GET /api/feedback/history` endpoint.
- Test integration POST → GET.

**Handoff signal:** `pytest tests/test_feedback.py -q` pass.

---

### Day 5 — Fri 20/05 · Sky Engine — chiêm tinh phương Tây (lớp tính)

**Mục tiêu:** Tính được vị trí 10 thiên thể + aspects + retrograde, deterministic 100%.

> **Đọc spec trước:** [spec-universe-now-western-astrology.md](spec-universe-now-western-astrology.md), đặc biệt mục 3 (quyết định kỹ thuật) và mục 7 (test bắt buộc).

- Cài `skyfield` (MIT). **Cấm** `pyswisseph` (AGPL) — xem mục 8 spec.
- Viết `scripts/download_ephemeris.sh` để tải JPL DE440 vào `data/ephemeris/` (gitignore).
- Tạo `engine/sky.py`:
  - `calculate_sky_chart(timestamp_utc, lat=None, lon=None) -> SkyChart`
  - Hàm con: positions → signs → aspects → retrograde flags → dominant element.
  - Tự viết thin layer cho sign/aspect math (không import lib AGPL).
- Tạo dataclasses: `SkyBody`, `SkyAspect`, `SkyChart` theo spec mục 4.
- Test (`tests/test_sky_engine.py`):
  - Sun ở 0° Aries lúc vernal equinox 2026 (±0.1°).
  - Mercury retrograde detect đúng cửa sổ đã biết 2026.
  - 2 hành tinh cách 91° → square orb 1°.
  - Determinism: cùng UTC → output bytes identical.

**Deliverable:**
- `engine/sky.py` + dataclasses.
- `scripts/download_ephemeris.sh`.
- `tests/test_sky_engine.py` — 4 test pass.
- `requirements.txt` cập nhật (skyfield).

**Handoff signal:** `pytest tests/test_sky_engine.py -q` pass.

**Quyết định cần xác nhận trong ngày này** (đánh dấu vào bảng spec mục 3):
- [ ] Skyfield là lựa chọn cuối?
- [ ] House system: Placidus hay Whole Sign cho v1?
- [ ] Aspect minor có ở v1 không?

---

### Day 6 — Sat 21/05 · Universe Now nâng cấp đầy đủ (API + UI + collectors)

**Mục tiêu:** Tích hợp Sky Engine vào Trang 1 + nối NOAA/JPL thực + UI hoàn chỉnh.

**Backend:**
- Thêm endpoint `GET /api/sky-now` (xem spec mục 5.1).
- Mở rộng `/api/universe-now` thêm field `sky` (xem spec mục 5.2, backward compatible).
- Nối collector thực:
  - `collectors/noaa_swpc.py` gọi NOAA Kp với timeout + fallback.
  - `collectors/jpl_horizons.py` — kiểm tra dùng được hay thay bằng skyfield local.
  - Flag `kp_source` và `ephemeris_source` rõ trong response.

**Frontend:**
- Tạo `client/webapp/src/components/SkyChartPanel.vue`:
  - SVG 2D vòng 12 cung + 10 hành tinh + đường aspect.
  - Tooltip hover: cung/độ/retrograde/dignity (hành tinh), loại + orb (aspect).
- Nối vào Trang 1 dưới `EnergyWeatherPanel`.
- Tuân thủ copy rules mục 6.3 spec — nhãn nhỏ "đây là mô hình trạng thái".

**Test:**
- `tests/test_api_sky.py` — `/api/sky-now` 200 + schema khớp.
- `tests/test_api.py` — backward compat `/api/universe-now`.
- Mock NOAA: success → `kp_source: "noaa_swpc"`; fail → `kp_source: "fallback"`.

**Deliverable:**
- 2 endpoint mới/mở rộng.
- `SkyChartPanel.vue` hoạt động trên trình duyệt.
- 5+ test mới pass.
- `docs/api-contracts.md` cập nhật.

**Handoff signal:** `PYTHONPATH=. pytest tests -q` toàn bộ pass + browser thấy SkyChartPanel render.

---

### Day 7 — Sun 22/05 · Test E2E + Handoff + Release note

**Mục tiêu:** Chạy luồng người dùng đầy đủ và để lại bàn giao sạch sẽ cho tuần 3.

**E2E test (`tests/test_e2e_flow.py`):**
1. `GET /api/universe-now` → có `sky` field.
2. `GET /api/sky-now` → 10 hành tinh + ≥3 aspects.
3. `POST /api/personal-profile` (ngày sinh cố định) → vector + milestones.
4. Lấy `prompt_id` từ milestone đầu tiên.
5. `POST /api/feedback` với `feedback_value: "đúng"` → `201`.
6. `GET /api/feedback/history` → có bản ghi vừa gửi.

**Handoff:**
- `docs/session-handoff-2026-05-22.md`:
  - Tổng kết tuần (cái đã xong, cái còn dở).
  - Snapshot test cuối: `pytest -q` output.
  - Top 3 backlog cho tuần 3.
  - Câu hỏi mở từ spec chiêm tinh (mục 11) — đã trả lời cái nào?
- `docs/weekly-plan-2026-05-23-to-2026-05-29.md` — khung trống cho tuần 3.
- `docs/release-note-2026-05-22.md` — nội dung công khai cho team.

**Deliverable:**
- Test E2E pass.
- 3 file handoff/release/plan tuần sau.

**Handoff signal:** Toàn bộ test suite pass, file bàn giao đầy đủ.

---

## 4) Backlog ưu tiên cao (nếu còn thời gian)

1. **Nối `core/yi64` pipeline vào runtime flow** — thay logic năm trong main.py.
2. **Thêm nguồn sách thứ 3 cho 64 quẻ** — tăng độ phong phú diễn giải.
3. **House system Placidus** cho Sky Engine (nếu Whole Sign chốt cho v1).
4. **UniverseCore.vue 3D** — chỉ làm nếu các text-based panel đã ổn định.
5. **Schema versioning cho seed data** — v1, v2 metadata.

---

## 5) Tiêu chí done cho cả tuần

### Track A — Product Core
- [ ] `five_element_bias` tính từ Can-Chi thực, deterministic.
- [ ] `generate_davan_windows` thay thế hardcode.
- [ ] `scoring.py` / `resonance.py` trả 3 trục Thiên–Địa–Nhân.
- [ ] `GET /api/feedback/history` hoạt động.

### Track B — Western Astrology
- [ ] `engine/sky.py` + tests pass theo spec mục 7.
- [ ] `GET /api/sky-now` trả đủ data.
- [ ] `SkyChartPanel.vue` render đúng trên browser.
- [ ] License clean: không có lib AGPL.
- [ ] Copy rule (no fortune-telling) tuân thủ — review thủ công.

### Track C — Stabilize
- [ ] Test E2E 3 màn hình pass.
- [ ] `PYTHONPATH=. pytest tests -q` — không regressions.
- [ ] File handoff + release note + plan tuần 3 đầy đủ.

---

## 6) Lệnh kiểm tra nhanh (chạy mỗi khi pick up giữa tuần)

```bash
source .venv/bin/activate
PYTHONPATH=. pytest tests -q --tb=no | tail -10
python scripts/safety_scan.py
git status
git log --oneline -5
```

```bash
./scripts/dev-up.sh
# Sau đó:
# - http://127.0.0.1:5173  (Trang 1 / Universe Now)
# - http://127.0.0.1:8000/docs (FastAPI Swagger)
```

---

## 7) Decision log (cập nhật trong tuần)

| Ngày | Quyết định | Người | Ghi chú |
|---|---|---|---|
| 2026-05-10 | Thêm Track B — Western Astrology vào tuần 2 | CEO | Spec đầy đủ ở `spec-universe-now-western-astrology.md` |
| 2026-05-10 | License: cấm `pyswisseph` (AGPL), dùng `skyfield` (MIT) | (chưa xác nhận) | Cần tech lead chốt |
| | | | |

---

## 8) Open handoff (cập nhật khi pause/bàn giao)

> Khi bạn pause hoặc bàn giao cho người khác, **xóa block dưới và thay bằng tình trạng hiện tại**.

```
Last updated: 2026-05-10
Active owner: chưa gán
Current day: chưa bắt đầu (kế hoạch tuần 2)
Last test result: 52 passed (baseline tuần 1)
Files in progress: none
Blockers: none
Next action: chốt owner cho Track A và Track B trước Mon 16/05
Open questions:
  - Tech lead xác nhận skyfield (Day 5)?
  - House system v1: Placidus hay Whole Sign?
  - Designer cho SkyChartPanel.vue đã có ai chưa?
```
